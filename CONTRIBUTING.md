# Contributing

Issues and focused pull requests are welcome.

Before changing a metric, read `docs/METRICS.md`. A Ginmap number must have a stable definition and a defensible GitHub data source. Do not introduce opaque developer scores or claims about developer quality.

Run before opening a pull request:

```bash
npm run lint
npm test
npm run typecheck
npm run build
```
